---
phase: 04.1-students-hub-restructure
plan: "01"
subsystem: css
tags: [css, layout, students-hub, accordion, sidebar, toggle]
dependency_graph:
  requires: []
  provides: [students-hub-section-19-css]
  affects: [students.html]
tech_stack:
  added: []
  patterns: [data-layout-attribute-selector, prefers-reduced-motion-guard, aria-checked-toggle, sticky-sidebar, max-height-accordion]
key_files:
  created: []
  modified:
    - css/style.css
decisions:
  - "Section 19 appended after existing Section 18 (contact styles) at line 1286 — preserves all existing styles"
  - "Layout 2 display:flex/flex-direction:column rule placed in 19e using rule specificity to override 19a display:none"
  - "prefers-reduced-motion wraps only accordion max-height transitions (not hover transitions per Phase 01 decision)"
metrics:
  duration: "5 minutes"
  completed: "2026-03-27"
  tasks_completed: 1
  files_modified: 1
---

# Phase 04.1 Plan 01: Students Hub Dual Layout System CSS Summary

Section 19 CSS appended to style.css providing layout toggle switch, Layout 1 sticky sidebar navigation, and Layout 2 expandable accordion cards for the dual-layout Students Hub comparison.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add Section 19 CSS for layout toggle, Layout 1 sidebar, and Layout 2 accordion | 92b21fa | css/style.css |

## What Was Built

**Section 19** added to `css/style.css` (lines 1286-1608), covering:

- **19a Layout visibility**: `[data-layout="1"]` and `[data-layout="2"]` attribute selectors control which layout container is visible
- **19b Layout toggle switch**: Pill-shaped button using `role="switch"` / `aria-checked` for active state styling; Meliora Blue `focus-visible` outline
- **19c Layout 1 sidebar**: CSS Grid `240px 1fr` two-column layout; `position: sticky` sidebar; sidebar nav items with yellow left-border active indicator
- **19d Layout 1 mobile**: `display: none` by default select wrapper; `@media (max-width: 767px)` collapses to single column and shows select dropdown
- **19e Layout 2 accordion**: `max-height: 0` collapsed state; `.is-open` class sets `max-height: 800px`; `+` icon rotates 45 degrees when expanded

## Verification

All acceptance criteria confirmed:

- `19. Phase 04.1` section header present (line 1288)
- `.layout-toggle__btn[aria-checked="false/true"]` selectors present
- `[data-layout="1"] .students-layout-1 { display: block; }` and Layout 2 counterpart present
- `.layout-1__wrapper` uses `grid-template-columns: 240px 1fr` with `align-items: start`
- `.layout-1__sidebar` uses `position: sticky`
- `.sidebar-nav__item[aria-selected="true"]` uses `border-left-color: var(--color-yellow)`
- `.layout-1__mobile-select-wrapper` hidden by default, shown at `max-width: 767px`
- `.topic-card__trigger` uses `font-family: var(--font-serif)`
- `.topic-card__content` has `max-height: 0`; `.is-open` has `max-height: 800px`
- `@media (prefers-reduced-motion: no-preference)` wraps accordion max-height transitions
- `.topic-card__trigger[aria-expanded="true"] .topic-card__icon` has `transform: rotate(45deg)`
- `@media (max-width: 767px)` hides sidebar, shows mobile select
- All interactive elements use `outline: 3px solid var(--color-meliora-blue)` on `focus-visible`
- Existing Section 13 tab styles (lines 658-824) not modified

## Deviations from Plan

None. Plan executed exactly as written.

## Self-Check: PASSED

- css/style.css modified and committed at 92b21fa
- All grep checks for required classes return matches
- File grew by 324 lines (slightly more than the estimated 180-220 due to extra blank lines between subsections for readability)
