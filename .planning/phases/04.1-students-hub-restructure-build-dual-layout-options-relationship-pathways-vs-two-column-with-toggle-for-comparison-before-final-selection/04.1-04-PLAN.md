---
phase: 04.1-students-hub-restructure
plan: 04
type: execute
wave: 3
depends_on:
  - 04.1-01
  - 04.1-02
  - 04.1-03
files_modified: []
autonomous: false
requirements:
  - STU-01
  - STU-02
  - STU-03
  - STU-04
  - STU-05
  - STU-06
  - STU-07
  - STU-08
  - STU-09
  - STU-10
  - STU-11
  - STU-12

must_haves:
  truths:
    - "Both layouts render correctly and all 4 topics are accessible in each"
    - "Toggle switches between layouts and choice persists after refresh"
    - "Mobile layout is usable at 320px and 640px widths"
    - "Keyboard navigation works for both layouts"
    - "All content is in first-person voice with no em-dashes"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification checkpoint for the complete dual-layout Students Hub. User tests both layouts, the toggle, mobile responsiveness, and accessibility before proceeding to Phase 5.

Purpose: Confirm both layouts work correctly and the user can begin comparing them for a final selection decision.
Output: User approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/ibarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ibarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-students-hub-restructure-build-dual-layout-options-relationship-pathways-vs-two-column-with-toggle-for-comparison-before-final-selection/04.1-01-SUMMARY.md
@.planning/phases/04.1-students-hub-restructure-build-dual-layout-options-relationship-pathways-vs-two-column-with-toggle-for-comparison-before-final-selection/04.1-02-SUMMARY.md
@.planning/phases/04.1-students-hub-restructure-build-dual-layout-options-relationship-pathways-vs-two-column-with-toggle-for-comparison-before-final-selection/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated validation checks on students.html</name>
  <files></files>
  <read_first>
    - students.html
    - css/style.css (lines 1280+ for Section 19)
    - js/main.js (verify all 3 new functions exist)
  </read_first>
  <action>
Run a series of automated checks to validate the implementation before user visual verification:

1. **HTML structure checks:**
   - Confirm `data-layout` attribute exists on `.students-content` wrapper
   - Confirm exactly 4 `sidebar-nav__item` buttons exist
   - Confirm exactly 4 `layout-1__panel` divs exist
   - Confirm exactly 4 `topic-card__trigger` buttons exist
   - Confirm exactly 4 `topic-card__content` divs exist
   - Confirm `role="switch"` on toggle button
   - Confirm `aria-current="page"` on Students nav link
   - Confirm no em-dashes (`---`, `--`, `&mdash;`, or the literal em-dash character) in page content

2. **CSS validation:**
   - Confirm Section 19 exists in css/style.css
   - Confirm `[data-layout="1"]` and `[data-layout="2"]` selectors exist
   - Confirm `.layout-toggle__btn` styles exist
   - Confirm `.layout-1__wrapper` with `grid-template-columns` exists
   - Confirm `.topic-card__content` with `max-height` exists
   - Confirm `prefers-reduced-motion` guard wraps transitions

3. **JavaScript validation:**
   - Confirm `initLayoutToggle` function exists
   - Confirm `initSidebarNav` function exists
   - Confirm `initExpandableCards` function exists
   - Confirm all three are called in DOMContentLoaded
   - Confirm `ibarron:studentsLayout` localStorage key is used

4. **Content checks:**
   - Confirm first-person pronouns (I, my, me) appear in content
   - Confirm no third-person references ("Dr. Barron", "he", "his")
   - Confirm all 4 topic names appear: "Get Started", "Collaboration", "Professional Recommendations", "Student Impact"

Report results as a checklist with PASS/FAIL for each item.
  </action>
  <verify>
    <automated>grep -c 'data-layout=' students.html | grep -q "[1-9]" && grep -c 'role="switch"' students.html | grep -q "1" && grep -c 'initLayoutToggle' js/main.js | grep -q "[1-9]" && grep -c 'initSidebarNav' js/main.js | grep -q "[1-9]" && grep -c 'initExpandableCards' js/main.js | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All HTML structure checks pass (4 sidebar buttons, 4 panels, 4 cards, role="switch", aria-current)
    - All CSS checks pass (Section 19 exists, data-layout selectors, grid, max-height, motion guard)
    - All JS checks pass (3 functions exist, DOMContentLoaded wiring, localStorage key)
    - No em-dashes found in students.html content
    - First-person voice confirmed present
  </acceptance_criteria>
  <done>Automated validation checklist complete with all items passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of dual-layout Students Hub</name>
  <files></files>
  <action>
Present the user with a structured verification checklist for both layouts. The user opens students.html in their browser and tests the following scenarios. Record their approval or issues.
  </action>
  <what-built>Complete dual-layout Students Hub with toggle comparison mechanism, sidebar navigation (Layout 1), expandable accordion cards (Layout 2), mobile responsive breakpoints, and localStorage persistence.</what-built>
  <how-to-verify>
    1. Open students.html in Chrome
    2. You should see Layout 2 (expandable cards) by default with "Get Started" pre-expanded
    3. Click the toggle switch at the top. Page should switch to Layout 1 (sidebar navigation)
    4. In Layout 1: click each of the 4 sidebar buttons. The right panel should change accordingly
    5. Click toggle again to return to Layout 2 (cards)
    6. In Layout 2: click each card title. Cards should expand/collapse with smooth animation
    7. Refresh the page. Your last chosen layout should persist (try each layout, refresh, confirm)
    8. Open Chrome DevTools, set viewport to 320px wide:
       - Layout 1: sidebar should be hidden, a "Jump to topic" select dropdown should appear
       - Layout 2: cards should stack vertically in a single column
    9. At 320px in Layout 1: use the select dropdown to switch topics. Content should change.
    10. Set viewport to 768px: Layout 1 sidebar should reappear, select dropdown should hide
    11. Tab through interactive elements with keyboard:
        - Toggle button should have visible focus ring
        - Layout 1 sidebar buttons should be Tab-navigable
        - Layout 2 card triggers should be Tab-navigable, Enter/Space should expand/collapse
    12. Check content: all copy should use "I/my/me", no em-dashes, no "Current/Prior/Prospective Students"
    13. Check that nav highlights "Students" link as active page
    14. Open other pages (index.html, research.html) to verify no visual regressions
  </how-to-verify>
  <verify>User confirms all 14 verification steps pass or provides issue list</verify>
  <done>User approves both layouts as functional and ready for layout comparison/selection.</done>
  <resume-signal>Type "approved" to proceed to Phase 5, or describe any issues to address</resume-signal>
</task>

</tasks>

<verification>
- Automated checks pass for HTML structure, CSS classes, JS functions
- User visually confirms both layouts, toggle, mobile, and keyboard navigation
</verification>

<success_criteria>
- User approves both layouts as functional and ready for comparison
- All automated validation checks pass
- No regressions on other pages
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-students-hub-restructure-build-dual-layout-options-relationship-pathways-vs-two-column-with-toggle-for-comparison-before-final-selection/04.1-04-SUMMARY.md`
</output>
