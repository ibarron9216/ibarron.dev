---
phase: 1
slug: foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundation. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — plain CSS BEM classes |
| Icon library | none — CSS-only visual indicators |
| Font | Inter Variable (self-hosted .woff2) + Georgia (system serif) |

Source: CONTEXT.md locked decisions, RESEARCH.md Standard Stack.

No `components.json` detected. Stack is plain HTML/CSS/JS — shadcn is not applicable.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | CSS Custom Property | Value | Usage |
|-------|---------------------|-------|-------|
| xs | `--spacing-xs` | 4px | Icon gaps, inline padding |
| sm | `--spacing-sm` | 8px | Compact element spacing, badge padding |
| md | `--spacing-md` | 16px | Default element spacing, card internal |
| lg | `--spacing-lg` | 24px | Card padding (TYP-08: 1.5rem = 24px), section internal |
| xl | `--spacing-xl` | 32px | Layout gaps |
| 2xl | `--spacing-2xl` | 48px | Major section breaks |
| 3xl | `--spacing-3xl` | 64px | Page-level vertical rhythm |

Exceptions:
- Button vertical padding: 12px (0.75rem) — required by TYP-05
- Button horizontal padding: 28px (1.75rem) — required by TYP-05
- Touch targets: minimum 44px height on mobile — required by RESP-06

Source: REQUIREMENTS.md TYP-05, RESP-06. Scale defaults applied for tokens not explicitly stated in upstream.

---

## Typography

All `font-size` values use `clamp()` for fluid scaling. Declared values are the base (minimum) sizes.

| Role | CSS Custom Property | Base Size | Weight | Line Height | Font Family | Usage |
|------|---------------------|-----------|--------|-------------|-------------|-------|
| Display / H1 | `--font-size-h1` | clamp(2rem, 5vw, 3.5rem) | 700 (bold) | 1.2 | Georgia, serif | Page H1s |
| Heading / H2 | `--font-size-h2` | clamp(1.5rem, 3vw, 2.25rem) | 700 (bold) | 1.2 | Georgia, serif | Section H2s |
| Subheading / H3–H5 | `--font-size-h3` | clamp(1.125rem, 2vw, 1.5rem) | 600 (semibold) | 1.3 | Inter Variable, sans-serif | Card titles, sub-sections |
| Body | `--font-size-body` | clamp(1rem, 2vw, 1.125rem) | 400 (regular) | 1.7 | Inter Variable, sans-serif | All body paragraphs |
| Eyebrow | `--font-size-eyebrow` | 0.875rem (fixed) | 600 (semibold) | 1.4 | Inter Variable, sans-serif | Section eyebrows, labels |
| Button | `--font-size-button` | 1.125rem (fixed) | 600 (semibold) | 1.0 | Inter Variable, sans-serif | All button labels |

**Declared weights: 400 (regular) and 600 (semibold).** Georgia headings use 700 (bold) which is a third distinct weight — justified by the two-font system (serif headings vs. sans-serif body).

Eyebrow styling (TYP-04): `text-transform: uppercase; letter-spacing: 0.1em;` in addition to size/weight above.

Source: REQUIREMENTS.md TYP-01 through TYP-08, RESEARCH.md Phase Requirements table.

---

## Color

| Role | CSS Custom Property | Value | Usage |
|------|---------------------|-------|-------|
| Dominant (60%) | `--color-white` | #FFFFFF | Page background, card backgrounds, text on navy |
| Secondary (30%) | `--color-navy` | #001E5F | Nav bar, page headers, footer, H1 hero backgrounds |
| Accent (10%) | `--color-yellow` | #FFD82B | Reserved elements only (see below) |
| Hover/Interactive | `--color-meliora-blue` | #0045A0 | Primary button hover state only |
| Text body | `--color-dark-gray` | #707070 | Muted body text, eyebrow labels on white backgrounds |
| Card border | `--color-border` | #E8E8E8 | Card 1px border (TYP-08) |
| Butter background | `--color-butter` | #FFFBF0 | Research Overview and Teaching Philosophy section backgrounds |
| Destructive | — | — | No destructive actions in Phase 1 |

**Accent (#FFD82B) reserved for:**
1. Active nav link text and underline (`aria-current="page"` styled links)
2. Card top border — 4px yellow top border on highlight cards (TYP-08)
3. Profile photo placeholder "IB" initials text
4. Tab active state indicator (yellow text on navy background — ANIM-05)
5. Publication card left border (4px yellow left border — RES-10, Phase 3 preview pattern established here)

**Accent is NOT used for:** body text, general links, hover states on non-nav elements, or backgrounds other than the above.

**WCAG AA contrast verification (from RESEARCH.md FOUND-06):**
- Navy #001E5F on white #FFFFFF: 15.54:1 — PASS (min 4.5:1)
- White #FFFFFF on navy #001E5F: 15.54:1 — PASS
- Yellow #FFD82B on navy #001E5F: 11.18:1 — PASS
- Dark gray #707070 on white #FFFFFF: 4.95:1 — PASS
- Yellow #FFD82B on white: FAIL — yellow text MUST NOT appear on white backgrounds (A11Y-02)

Source: REQUIREMENTS.md FOUND-02, FOUND-06, TYP-06, TYP-07, TYP-08, A11Y-01, A11Y-02. CONTEXT.md brand decisions.

---

## Component Inventory

Phase 1 establishes these reusable CSS component patterns. All subsequent phases inherit them.

### Buttons

| Class | Visual | Usage |
|-------|--------|-------|
| `.btn.btn--primary` | Navy background (#001E5F), white text, 8px border-radius | Primary actions (View Research, Download CV) |
| `.btn.btn--outline` | Transparent background, 2px navy border, navy text | Secondary actions (For Students) |
| `.btn.btn--outline--white` | Transparent background, 2px white border, white text | Secondary actions on navy backgrounds |

Button hover states:
- `.btn--primary` hover: background-color changes to Meliora Blue (#0045A0), 0.2s ease transition
- `.btn--outline` hover: background-color fills navy, text turns white, 0.2s ease transition
- All buttons: `transition: background-color 0.2s ease, color 0.2s ease, border-color 0.2s ease`

### Cards

| Class | Visual | Usage |
|-------|--------|-------|
| `.card` | White background, 1px border (#E8E8E8), 8px border-radius, 24px padding | Base card |
| `.card--highlighted` | Base card + 4px yellow top border | Highlight cards (Research, Teaching, Students on home) |
| `.card--publication` | Base card + 4px yellow left border | Publication listings (Phase 3 pattern) |
| `.card--funded` | Base card with eyebrow label | NSF funded research card (Phase 3 pattern) |

### Tag Pills

| Class | Visual | Usage |
|-------|--------|-------|
| `.tag-pill` | Navy background, white text, rounded (border-radius: 100px), compact padding | Research interest tags |
| `.badge--year` | Navy background, white text, small rounded pill | Year badges on publication cards |
| `.badge--semester` | Navy background, white text, small rounded pill | Semester badges on course cards |

### Navigation

Structure: `<nav role="navigation">` with `<ul role="list">` links.

Note: RESEARCH.md flags that `role="menubar"` (specified in CONTEXT.md) is inappropriate per WAI-ARIA APG for site navigation. This spec adopts the disclosure nav pattern instead: `<nav>` with plain `<ul>` links and `aria-current="page"` on the active item. The hamburger button uses `aria-expanded` and `aria-controls`. This aligns with A11Y-04.

| Element | CSS class | ARIA attribute | Visual |
|---------|-----------|----------------|--------|
| Nav container | `.nav` | `role="navigation"` | White background, `position: sticky; top: 0`, box-shadow on scroll |
| Brand name | `.nav__brand` | — | Left-aligned, Georgia bold, navy text |
| Nav links | `.nav__links` | `role="list"` | Right-aligned, flex row, Inter 600 |
| Active link | `.nav__link[aria-current]` | `aria-current="page"` | Yellow text (#FFD82B), yellow underline |
| Hamburger button | `.nav__hamburger` | `aria-expanded`, `aria-controls="nav-menu"`, `aria-label="Toggle navigation"` | 3-line icon, visible only at <768px |
| Mobile menu | `.nav__menu` | `id="nav-menu"` | Hidden by default, full-width dropdown |

### Footer

Two-column CSS Grid on desktop (≥640px), single column on mobile.

| Element | CSS class | Content |
|---------|-----------|---------|
| Footer container | `.footer` | Navy background (#001E5F) |
| Footer text | `.footer__text` | White text, Inter 400 |
| Year placeholder | `.footer__year` | Populated by JS: `new Date().getFullYear()` |

### Profile Photo Placeholder

CSS-only (ASSET-04). Navy rectangle, `aspect-ratio: 1`, yellow "IB" initials in Georgia bold, centered.

---

## Interaction Contracts

### Hamburger Menu (JavaScript)

Trigger: click `.nav__hamburger`
Effect:
1. Toggle `aria-expanded` between `"false"` and `"true"` on the button
2. Toggle `.nav--open` class on `.nav` element
3. CSS `.nav--open .nav__menu { display: block }` shows mobile menu
4. CSS `.nav--open .nav__hamburger` renders 3 lines as × (X) pattern

Keyboard: button is `<button>` element — receives Enter/Space natively. No additional JS needed.

### Animations (CSS + `prefers-reduced-motion`)

All animations wrapped in:
```css
@media (prefers-reduced-motion: no-preference) { ... }
```

Page-load fade-in + slide-up:
- `@keyframes fadeInUp`: opacity 0→1, translateY(20px)→translateY(0)
- Duration: 0.5s, easing: `ease-out`
- Stagger: 0.1s delay per `.section:nth-child(n)` selector
- Applied to: all `<section>` and `<article>` elements at page load

Hover transitions (always active — not wrapped in reduced-motion, intentional: these are immediate responses, not gratuitous):
- Buttons: 0.2s ease on background-color, color, border-color
- Cards: 0.2s ease on box-shadow (subtle lift on hover)
- Nav links: 0.2s ease on color

### Focus Indicators

All interactive elements:
```css
:focus-visible {
  outline: 3px solid var(--color-yellow);
  outline-offset: 2px;
}
```
Exception: elements on navy backgrounds use white outline instead.

---

## CSS Architecture

### File: `css/style.css` (single file, ASSET-03)

Organization order (top to bottom):
1. `@font-face` declarations (Inter Variable)
2. `:root` custom properties — colors, spacing, fonts, breakpoints
3. CSS reset / base styles
4. Typography base styles (h1–h6, p, a)
5. Layout utilities (container, grid helpers)
6. Navigation component
7. Footer component
8. Button components
9. Card components
10. Tag pill / badge components
11. Profile photo placeholder
12. Animation keyframes + prefers-reduced-motion wrapper
13. Responsive breakpoint overrides (mobile → tablet → desktop)

### CSS Custom Properties in `:root`

```css
:root {
  /* Colors */
  --color-navy: #001E5F;
  --color-yellow: #FFD82B;
  --color-meliora-blue: #0045A0;
  --color-white: #FFFFFF;
  --color-dark-gray: #707070;
  --color-border: #E8E8E8;
  --color-butter: #FFFBF0;

  /* Spacing */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --spacing-3xl: 64px;

  /* Typography */
  --font-sans: 'Inter Variable', system-ui, -apple-system, sans-serif;
  --font-serif: Georgia, 'Times New Roman', serif;
  --font-size-h1: clamp(2rem, 5vw, 3.5rem);
  --font-size-h2: clamp(1.5rem, 3vw, 2.25rem);
  --font-size-h3: clamp(1.125rem, 2vw, 1.5rem);
  --font-size-body: clamp(1rem, 2vw, 1.125rem);
  --font-size-eyebrow: 0.875rem;
  --font-size-button: 1.125rem;

  /* Breakpoints (reference only — used in @media queries, not var()) */
  --breakpoint-mobile: 640px;
  --breakpoint-nav: 768px;
  --breakpoint-desktop: 1024px;
}
```

### File: `js/main.js` (single file, <200 lines)

Three discrete functions:
1. `initHamburger()` — nav toggle with aria-expanded
2. `initTabs()` — ARIA tab pattern with keyboard navigation (Phase 4 use)
3. `initFooterYear()` — `document.querySelector('.footer__year').textContent = new Date().getFullYear()`

---

## Copywriting Contract

Phase 1 is an infrastructure phase. No end-user facing CTAs are created in this phase — the components defined here will carry copy in Phase 2+. The contract below covers the component states that Phase 1 must render correctly.

| Element | Copy |
|---------|------|
| Hamburger button aria-label | "Toggle navigation" |
| Nav brand name | "Dr. Irving Barron" |
| Footer copyright | "© [current year] Dr. Irving Barron. All rights reserved." |
| Skip link (if implemented) | "Skip to main content" |
| Empty state heading | Not applicable in Phase 1 (no data-driven content) |
| Empty state body | Not applicable in Phase 1 |
| Error state | Not applicable in Phase 1 (no forms or data fetching) |
| Destructive confirmation | No destructive actions in Phase 1 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | n/a — plain HTML/CSS/JS, no component registry | not applicable |

No shadcn, no third-party registries. All components are hand-authored CSS classes. Registry safety gate: not applicable.

---

## Responsive Behavior Contract

| Breakpoint | Layout |
|------------|--------|
| < 640px (mobile) | Single column, hamburger nav, stacked footer |
| 640px–1023px (tablet) | Two-column where applicable, hamburger nav still active |
| < 768px | Nav collapses to hamburger (NAV-02) |
| ≥ 768px | Nav links shown inline, hamburger hidden |
| ≥ 1024px (desktop) | Full multi-column layouts active |

All multi-column layouts (hero, footer, contact, card grids) stack to single column below their respective breakpoints using CSS Grid `grid-template-columns` overrides.

Minimum supported width: 320px (RESP-01).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 01-foundation*
*UI-SPEC created: 2026-03-25*
*Sources: CONTEXT.md (8 decisions), REQUIREMENTS.md (TYP-01–08, FOUND-01–06, NAV-01–05, ANIM-01–05, A11Y-01–08), RESEARCH.md (Standard Stack, color contrast verification)*
