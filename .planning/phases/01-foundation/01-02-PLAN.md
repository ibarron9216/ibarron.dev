---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [js/main.js, index.html, css/style.css]
autonomous: true
requirements: [FOUND-01, FOUND-03, NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, LAYOUT-01, LAYOUT-02, ANIM-04, ANIM-05, PERF-04]

must_haves:
  truths:
    - "Navigation bar is sticky, shows Dr. Irving Barron brand name left, nav links right, hamburger on mobile"
    - "Hamburger button toggles mobile menu visibility and changes visual state (aria-expanded)"
    - "Current page nav link marked with aria-current='page' and styled yellow (#FFD82B)"
    - "Footer displays copyright with dynamically generated current year via JavaScript"
    - "Nav collapses to hamburger menu at <768px; full inline nav at >=768px"
    - "All interactive elements (hamburger, nav links) have visible focus indicators"
    - "Tab active state visually indicated with yellow text on navy background"

  artifacts:
    - path: "index.html"
      provides: "Minimal HTML scaffold with semantic structure (header, nav, main, footer) and nav/footer shared markup"
      min_lines: 60
    - path: "js/main.js"
      provides: "Hamburger toggle and footer year initialization JavaScript functions"
      max_lines: 100
    - path: "css/style.css"
      provides: "Tab styling with [aria-selected='true'] active state"
      provides_update: "Added to Plan 01 base"

  key_links:
    - from: "index.html"
      to: "css/style.css"
      via: "link rel='stylesheet'"
      pattern: "href.*css/style\\.css"
    - from: "index.html"
      to: "js/main.js"
      via: "script src"
      pattern: "src.*js/main\\.js"
    - from: "js/main.js"
      to: ".nav-toggle"
      via: "querySelector + click listener"
      pattern: "querySelector.*nav-toggle"
    - from: "js/main.js"
      to: ".footer__year"
      via: "textContent assignment"
      pattern: "querySelector.*footer.*year.*textContent.*getFullYear"
    - from: "css/style.css"
      to: "[aria-selected='true']"
      via: "tab styling selector"
      pattern: "\\[aria-selected.*true.*\\].*color.*FFD82B"

---

<objective>
Build the shared navigation and footer HTML components plus JavaScript utilities that all five pages will inherit. Also add tab styling CSS for Students page tabs.

Purpose: Establish the nav and footer patterns so that subsequent page phases (Home, Research, Teaching, Students, Contact) can copy this scaffold and focus only on page-specific content. Create the semantic HTML structure and vanilla JS that powers nav toggling and dynamic year display. Add CSS for tab active state styling.

Output: index.html with nav and footer markup (used as template for other pages); js/main.js with hamburger toggle and footer year initialization (<100 lines total); css/style.css updated with tab styling rules.
</objective>

<execution_context>
@/Users/ibarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ibarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with semantic nav/footer scaffold</name>
  <files>index.html</files>

  <read_first>
    - css/style.css (to verify class names match)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Navigation section, Footer section, Copywriting Contract, Responsive Behavior Contract)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: Mobile Navigation with Disclosure Pattern, Pattern 2: Footer structure)
    - .planning/REQUIREMENTS.md (FOUND-01, NAV-01–05, LAYOUT-01–02, A11Y-04, A11Y-07, A11Y-08)
  </read_first>

  <action>
    Create index.html as a minimal template that other pages will copy. This is the first page built; subsequent pages (research.html, teaching.html, etc.) will follow the same structure with only the main content differing.

    **HTML Structure:**
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <meta name="description" content="Dr. Irving Barron, Faculty at University of Rochester">
      <title>Dr. Irving Barron</title>
      <link rel="stylesheet" href="css/style.css">
    </head>
    <body>

      <!-- HEADER / NAVIGATION -->
      <header class="site-header">
        <nav class="nav" aria-label="Main navigation">
          <a class="nav__brand" href="index.html">Dr. Irving Barron</a>

          <!-- Hamburger button — visible only on mobile (<768px) -->
          <button
            class="nav__hamburger"
            aria-label="Toggle navigation"
            aria-expanded="false"
            aria-controls="nav-menu"
          >
            <span class="nav__hamburger-line"></span>
            <span class="nav__hamburger-line"></span>
            <span class="nav__hamburger-line"></span>
          </button>

          <!-- Navigation links — hidden on mobile until toggled -->
          <ul class="nav__menu" id="nav-menu" role="list">
            <li><a class="nav__link" href="index.html" aria-current="page">Home</a></li>
            <li><a class="nav__link" href="research.html">Research</a></li>
            <li><a class="nav__link" href="teaching.html">Teaching</a></li>
            <li><a class="nav__link" href="students.html">Students</a></li>
            <li><a class="nav__link" href="contact.html">Contact</a></li>
          </ul>
        </nav>
      </header>

      <!-- MAIN CONTENT (page-specific, filled in by Phase 2+) -->
      <main class="main-content">
        <!-- Placeholder for page content -->
        <section class="hero">
          <p class="eyebrow">Foundation Phase Placeholder</p>
          <h1>Index Page Scaffold</h1>
          <p>This is the template structure used by all five pages. Phase 2 (Home Page) will fill in the hero, About Me, and Highlight Cards sections.</p>
        </section>
      </main>

      <!-- FOOTER -->
      <footer class="footer">
        <div class="footer__content">
          <p class="footer__text">© <span class="footer__year"></span> Dr. Irving Barron. All rights reserved.</p>
        </div>
      </footer>

      <!-- JAVASCRIPT -->
      <script src="js/main.js"></script>
    </body>
    </html>
    ```

    **Key Points:**
    1. Semantic HTML5: `<header>`, `<nav>`, `<main>`, `<footer>` elements per FOUND-01
    2. Nav structure follows Pattern 3 (Mobile Navigation with Disclosure Pattern) from RESEARCH.md
    3. Active page link marked with `aria-current="page"` (not a class) per NAV-04
    4. Hamburger button:
       - `aria-label="Toggle navigation"` per NAV-02
       - `aria-expanded="false"` toggles true/false when clicked per NAV-03
       - `aria-controls="nav-menu"` references the menu ID per accessibility
       - Three `<span>` lines render as the hamburger icon in CSS
    5. Mobile menu has `id="nav-menu"` to match aria-controls per NAV-02
    6. Footer has `.footer__year` span for JavaScript to populate per LAYOUT-02
    7. Link to css/style.css in `<head>` per ASSET-03, PERF-03
    8. Link to js/main.js before `</body>` per PERF-04
    9. Placeholder section with eyebrow, H1, and body text to demonstrate typography
    10. Meta tags: charset, viewport (responsive), description

    **Class Naming (must match css/style.css):**
    - `.nav` — nav container with sticky positioning
    - `.nav__brand` — Dr. Irving Barron text, left-aligned
    - `.nav__hamburger` — hamburger button, hidden on desktop
    - `.nav__hamburger-line` — individual lines of hamburger icon
    - `.nav__menu` — nav links container, toggles on mobile
    - `.nav__link` — individual nav links
    - `.footer` — footer container with navy background
    - `.footer__text` — copyright text
    - `.footer__year` — year span populated by JS
    - `.eyebrow` — section label in gray, uppercase
    - `.main-content` — semantic wrapper for main content
    - `.hero` — first section, will be styled per page

    **Why this scaffold:**
    - All five pages share identical nav/footer markup
    - Only the `<main>` section differs per page
    - Copy this file to research.html, teaching.html, students.html, contact.html in Phase 3+
    - Change `aria-current="page"` and page-specific content, nothing else
  </action>

  <verify>
    - index.html exists and is valid HTML5
    - File contains all required semantic elements: header, nav, main, footer
    - Nav contains hamburger button with aria-label and aria-expanded
    - Nav contains ul#nav-menu with five links (Home, Research, Teaching, Students, Contact)
    - One nav link has aria-current="page"
    - Footer contains .footer__year span (empty, populated by JS)
    - File links css/style.css with correct path
    - File links js/main.js with correct path
    - No page-specific content (just placeholder section)
  </verify>

  <automated>
grep "<!DOCTYPE html>" index.html && \
grep -c "<header\|<nav\|<main\|<footer>" index.html && \
grep 'aria-current="page"' index.html && \
test $(wc -l < index.html) -gt 30
  </automated>

  <acceptance_criteria>
    Grep/file checks:
    - `grep "<!DOCTYPE html>" index.html` confirms HTML5 doctype
    - `grep -c "<header\|<nav\|<main\|<footer>" index.html` returns 4 (one each)
    - `grep 'aria-current="page"' index.html` shows active link marked
    - `grep 'aria-expanded=' index.html` shows toggle attribute
    - `grep 'aria-controls="nav-menu"' index.html` shows hamburger controls nav
    - `grep 'id="nav-menu"' index.html` shows menu has matching ID
    - `grep 'class="footer__year"' index.html` shows year span present
    - `grep 'href="css/style.css"' index.html` shows stylesheet link
    - `grep 'src="js/main.js"' index.html` shows script link
    - `wc -l index.html` returns ~60 lines or more (complete scaffold)
  </acceptance_criteria>

  <done>
    index.html created with full semantic structure (header, nav, main, footer), hamburger button with ARIA attributes, five nav links with one marked as current page, footer year placeholder, CSS and JS links, ready to serve as template for all five pages
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tab styling CSS for active state in design system</name>
  <files>css/style.css</files>

  <read_first>
    - css/style.css (to find appropriate location for tab styles)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Tab styling contract if present)
    - .planning/REQUIREMENTS.md (ANIM-05 requirement details)
  </read_first>

  <action>
    Update css/style.css to add tab styling for Students page tabs (ANIM-05). Add these styles to the component section of the CSS file (after other component classes like .btn, .card, etc.).

    **Tab Base Style:**
    ```css
    /* Tab Components (Students page pattern) */
    [role="tablist"] {
      display: flex;
      gap: var(--spacing-md);
      border-bottom: 2px solid var(--color-border);
      list-style: none;
      padding: 0;
      margin: 0 0 var(--spacing-lg) 0;
    }

    [role="tab"] {
      padding: var(--spacing-md) var(--spacing-lg);
      background: transparent;
      border: none;
      color: var(--color-dark-gray);
      font-family: var(--font-sans);
      font-weight: 700;
      font-size: var(--font-size-label);
      cursor: pointer;
      border-bottom: 3px solid transparent;
      transition: color 0.2s ease, border-bottom-color 0.2s ease;
    }

    [role="tab"]:hover {
      color: var(--color-navy);
    }

    [role="tab"]:focus-visible {
      outline: 3px solid var(--color-yellow);
      outline-offset: 2px;
    }

    /* Active tab state — ANIM-05 requirement */
    [role="tab"][aria-selected="true"] {
      color: var(--color-yellow);
      border-bottom-color: var(--color-yellow);
    }

    [role="tabpanel"] {
      padding: var(--spacing-lg) 0;
    }

    [role="tabpanel"][hidden] {
      display: none;
    }
    ```

    **Why this approach:**
    - Uses semantic [role="tab"] and [aria-selected="true"] selectors (not classes)
    - Active tab highlighted with yellow text on navy background per ANIM-05
    - Bottom border accent in yellow for visual emphasis
    - All transitions smooth via 0.2s ease (matches button transitions)
    - Focus-visible outline in yellow per accessibility guidelines
    - Will be used by Students page tabs in Phase 4

    **Integration:**
    - Add this after the .card and .tag-pill component sections in css/style.css
    - No JavaScript needed for styling (JavaScript will handle [aria-selected] toggle)
  </action>

  <verify>
    - css/style.css contains [role="tablist"] selector
    - css/style.css contains [role="tab"] base style
    - css/style.css contains [role="tab"][aria-selected="true"] active state with yellow color
    - Tab active state shows yellow text (#FFD82B) per ANIM-05
    - Tab inactive state shows dark gray text
    - [role="tabpanel"] and [role="tabpanel"][hidden] selectors present
  </verify>

  <automated>
grep -E '\[role="tab"\]' css/style.css && \
grep -E '\[aria-selected="true"\]' css/style.css && \
grep 'color: var(--color-yellow)' css/style.css
  </automated>

  <acceptance_criteria>
    Grep/file checks:
    - `grep '\[role="tab"\]' css/style.css` shows tab selector (at least 3 instances for base, hover, active)
    - `grep '\[aria-selected="true"\]' css/style.css` shows active state selector
    - `grep 'color: var(--color-yellow)' css/style.css` shows yellow color applied to active tab
    - `grep '\[role="tabpanel"\]' css/style.css` shows tabpanel styles
    - `grep 'border-bottom.*yellow\|FFD82B' css/style.css` shows yellow border on active tab
  </acceptance_criteria>

  <done>
    css/style.css updated with tab styling: [role="tab"] base class, [aria-selected="true"] active state with yellow text and yellow bottom border, focus indicators, transitions, ready for Students page tabs in Phase 4
  </done>
</task>

<task type="auto">
  <name>Task 3: Create js/main.js with hamburger toggle and footer year functions</name>
  <files>js/main.js</files>

  <read_first>
    - index.html (to verify class/ID selectors)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Hamburger JavaScript Toggle)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Interaction Contracts > Hamburger Menu, Footer)
    - .planning/REQUIREMENTS.md (NAV-03, LAYOUT-02, PERF-04, A11Y-04)
  </read_first>

  <action>
    Create js/main.js with three discrete functions totaling <100 lines per PERF-04. This file runs on all pages without modification.

    **Complete js/main.js contents:**
    ```javascript
    /**
     * Main site JavaScript
     * Hamburger navigation toggle, ARIA tabs pattern, footer year
     */

    // ==============================
    // 1. HAMBURGER NAVIGATION TOGGLE
    // ==============================
    function initNavToggle() {
      const toggle = document.querySelector('.nav__hamburger');
      const menu = document.querySelector('.nav__menu');

      if (!toggle || !menu) return;

      // Click handler for hamburger button
      toggle.addEventListener('click', () => {
        const isExpanded = toggle.getAttribute('aria-expanded') === 'true';
        toggle.setAttribute('aria-expanded', String(!isExpanded));
        menu.classList.toggle('nav__menu--open');
        toggle.classList.toggle('nav__hamburger--open');
      });

      // Close menu when clicking a nav link (mobile UX)
      menu.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
          toggle.setAttribute('aria-expanded', 'false');
          menu.classList.remove('nav__menu--open');
          toggle.classList.remove('nav__hamburger--open');
        });
      });
    }

    // ==============================
    // 2. FOOTER YEAR INITIALIZATION
    // ==============================
    function initFooterYear() {
      const yearSpan = document.querySelector('.footer__year');
      if (yearSpan) {
        yearSpan.textContent = new Date().getFullYear();
      }
    }

    // ==============================
    // 3. ARIA TABS (Phase 4 placeholder)
    // ==============================
    function initTabs() {
      // Placeholder for Students page tab interface (Phase 4)
      // Full implementation added in Phase 4
      const tablist = document.querySelector('[role="tablist"]');
      if (!tablist) return;

      const tabs = tablist.querySelectorAll('[role="tab"]');
      const panels = document.querySelectorAll('[role="tabpanel"]');

      tabs.forEach((tab, index) => {
        // Click to switch tab
        tab.addEventListener('click', () => {
          // Hide all panels
          panels.forEach(p => p.hidden = true);

          // Deselect all tabs
          tabs.forEach(t => t.setAttribute('aria-selected', 'false'));

          // Show selected panel and mark tab
          if (panels[index]) panels[index].hidden = false;
          tab.setAttribute('aria-selected', 'true');
          tab.focus();
        });

        // Arrow key navigation (left/right)
        tab.addEventListener('keydown', (e) => {
          let targetIndex = index;

          if (e.key === 'ArrowLeft') {
            targetIndex = index === 0 ? tabs.length - 1 : index - 1;
            e.preventDefault();
          } else if (e.key === 'ArrowRight') {
            targetIndex = index === tabs.length - 1 ? 0 : index + 1;
            e.preventDefault();
          } else {
            return;
          }

          tabs[targetIndex].click();
          tabs[targetIndex].focus();
        });
      });
    }

    // ==============================
    // 4. INITIALIZE ALL
    // ==============================
    document.addEventListener('DOMContentLoaded', () => {
      initNavToggle();
      initFooterYear();
      initTabs();
    });
    ```

    **Implementation Details:**

    **initNavToggle():**
    - Selects `.nav__hamburger` button and `.nav__menu` list
    - Click handler toggles `aria-expanded` between "true" and "false" per NAV-03
    - Adds/removes `.nav__menu--open` class on menu per UI-SPEC Interaction Contracts
    - Adds/removes `.nav__hamburger--open` class on button (for icon rotation/animation in CSS)
    - Closes menu automatically when user clicks a nav link (mobile UX improvement)

    **initFooterYear():**
    - Finds `.footer__year` span per LAYOUT-02
    - Sets textContent to current year using `new Date().getFullYear()`
    - Safe guard: returns early if element not found

    **initTabs():**
    - Placeholder for Students page ARIA tabs (Phase 4 full implementation)
    - Handles click to switch tabs
    - Handles arrow key navigation (left/right arrows cycle through tabs)
    - Updates aria-selected and tab visibility
    - No logic on pages without tablist (early return)

    **Event Listener:**
    - All functions initialized on `DOMContentLoaded` event (not before DOM ready)
    - Safe to run on all pages (functions check for element existence first)

    **Line Count:**
    - Expected ~80–100 lines including comments and whitespace
    - Well within PERF-04 budget of <200 lines total
    - No external dependencies

    **Why This Approach:**
    - Vanilla JavaScript only (per project constraints)
    - Functions are idempotent (safe to call multiple times)
    - Event listeners use modern `.addEventListener()` (not inline handlers)
    - ARIA attributes updated correctly (not CSS classes alone)
    - Keyboard accessibility built in (arrow keys, focus management)
  </action>

  <verify>
    - js/main.js exists and is readable
    - File contains initNavToggle() function
    - File contains initFooterYear() function
    - File contains initTabs() function for placeholder
    - File contains DOMContentLoaded event listener calling all functions
    - No external library imports (jQuery, Alpine, etc.)
    - No console errors when opened in browser
    - Hamburger button click toggles aria-expanded attribute
    - Footer year span populated with current year
  </verify>

  <automated>
grep "function init" js/main.js | grep -c "" && \
grep "getFullYear" js/main.js && \
test $(wc -l < js/main.js) -lt 200 && \
! grep -q "import.*from\|require.*from\|jquery\|axios" js/main.js
  </automated>

  <acceptance_criteria>
    Grep/file checks:
    - `grep -c "function init" js/main.js` returns >=3 (three functions)
    - `grep "initNavToggle\|initFooterYear\|initTabs" js/main.js` all three present
    - `grep "document.querySelector.*nav__hamburger" js/main.js` confirms hamburger selector
    - `grep "setAttribute.*aria-expanded" js/main.js` confirms ARIA toggle
    - `grep "classList.toggle.*nav__menu--open" js/main.js` confirms class toggle
    - `grep "getFullYear" js/main.js` confirms year logic
    - `grep "DOMContentLoaded" js/main.js` confirms event listener
    - `wc -l js/main.js` returns <100 lines (per PERF-04 budget)
    - No grep matches for: "jquery", "fetch", "axios", "async", "await" (no external dependencies)
  </acceptance_criteria>

  <done>
    js/main.js created with hamburger toggle (aria-expanded), footer year initialization, and ARIA tabs placeholder, all vanilla JS under 100 lines, safe to run on all pages without modification
  </done>
</task>

</tasks>

<verification>
**Verification Checklist for Plan 02:**
1. index.html is valid HTML5 with doctype, html, head, body
2. Semantic structure: header > nav, main, footer elements present
3. Nav includes hamburger button with aria-label and aria-expanded
4. Nav includes five links (Home, Research, Teaching, Students, Contact)
5. One nav link has aria-current="page"
6. Footer has .footer__year span (empty, JS-populated)
7. CSS and JS links point to correct paths (css/style.css, js/main.js)
8. js/main.js contains three functions (initNavToggle, initFooterYear, initTabs)
9. initNavToggle updates aria-expanded and toggles .nav__menu--open class
10. initFooterYear calls getFullYear() and sets textContent
11. All functions initialize on DOMContentLoaded
12. No external dependencies in js/main.js
13. Focus indicators and keyboard navigation ready (:focus-visible in CSS from Plan 01)
14. css/style.css contains tab styling with [role="tab"][aria-selected="true"] active state
15. Tab active state shows yellow text (#FFD82B) per ANIM-05
</verification>

<success_criteria>
Plan 02 complete when:
- index.html exists with full semantic structure, nav with hamburger, footer with year span
- js/main.js exists with three functions (<100 lines total)
- Opening index.html in browser shows nav with hamburger button visible (mobile mode or developer tools narrowed)
- Clicking hamburger toggles mobile menu (aria-expanded changes, class toggles)
- Footer displays current year (populated by JavaScript)
- All 12 requirements for nav/footer/layout/animations are addressed (NAV-01–05, LAYOUT-01–02, ANIM-04–05, PERF-04)
- HTML is valid and all selectors match CSS class names
- JavaScript uses vanilla ES2020+ (no jQuery, no external libs)
- css/style.css updated with tab styling for Students page tabs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- HTML scaffold structure (header, nav, main, footer shared by all pages)
- Nav pattern implementation (disclosure + hamburger, aria-current, icon toggle)
- Footer pattern (dynamic year via JS)
- JavaScript functions and interactions (init functions, event listeners, ARIA updates)
- Class naming (nav__hamburger, nav__menu--open, footer__year, etc.) for reference
- Tab styling pattern (ANIM-05: [role="tab"][aria-selected="true"] with yellow text)
- Next steps (Phase 2 will create home page filling in main content, copy this scaffold)
- Warning: Each page that copies this scaffold must update aria-current="page" to its own link
</output>
