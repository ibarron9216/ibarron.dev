---
phase: 04-students-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [css/style.css, students.html]
autonomous: true
requirements: [STU-01, STU-02, STU-03, STU-05, STU-08, STU-09, STU-10, STU-11, STU-12]
user_setup: []

must_haves:
  truths:
    - "Students page displays navy header with H1 'Students Hub' and lead text 'Resources for current, prior, and prospective students'"
    - "Three tab buttons are visible and clickable at all screen sizes (320px+)"
    - "Each tab button has correct styling: active tab has navy background and white text, inactive tabs have transparent background and navy text"
    - "Tab panels contain their respective content organized semantically (office hours, Blackboard link, email for Current; recommendation requirements for Prior; undergraduate research for Prospective)"
  artifacts:
    - path: css/style.css
      provides: "Tab interface styling (.tabs, [role='tab'], [role='tabpanel'], fade animation classes)"
      min_lines: 50
    - path: students.html
      provides: "Complete Students Hub page with header, tab navigation, and three content panels"
      exports: ["aria-label on hamburger", "aria-current on nav link", "role='tablist'", "role='tab'", "role='tabpanel'"]
  key_links:
    - from: css/style.css
      to: "[role='tab'] selectors"
      via: "CSS tab button styling"
      pattern: "\\[role=\"tab\"\\].*{.*background"
    - from: students.html
      to: css/style.css
      via: "class references in tab markup"
      pattern: "class=\".*tabs\""
    - from: students.html
      to: ARIAL roles
      via: "semantic tab structure"
      pattern: "role=\"tablist\""
---

<objective>
Build the Students Hub HTML page structure and CSS tab interface styling. This establishes the visual foundation and semantic markup for the three-section tab interface. No JavaScript interactivity added in this plan — that comes in Plan 02.

Purpose: Create the static HTML/CSS foundation that plan 02 will enhance with interactive JavaScript (click handlers, fade animations, tabindex management, keyboard navigation).

Output: students.html file and CSS tab styling rules in style.css
</objective>

<execution_context>
@/Users/ibarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ibarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-students-hub/04-CONTEXT.md
@.planning/phases/04-students-hub/04-RESEARCH.md
</context>

<interfaces>
## Existing CSS Components Available for Reuse

From css/style.css (already defined):

**Styling Patterns:**
- `.section` — Base section wrapper (64px vertical padding)
- `.section--navy` — Navy background sections (used for page headers)
- `.section--navy h1, h2, h3, h4, h5, p` — All text inside navy sections is white
- `.section--navy .eyebrow` — Eyebrow text on navy backgrounds is yellow (#FFD82B)
- `.container` — Centered max-width container (1100px max-width, 16px horizontal padding)
- `.card` — Base card component (white background, 1px border)
- `.card--highlighted` — Card with 4px yellow top border

**Typography:**
- `h1, h2` — Georgia serif, bold, `var(--font-size-heading)` (fluid: clamp 1.5rem-3.5rem)
- `h3, h4, h5` — Inter sans-serif, bold, `var(--font-size-subheading)` (fluid: clamp 1.125rem-1.5rem)
- `.eyebrow` — Inter 600, 0.875rem, uppercase, letter-spacing 0.1em, dark gray color

**Spacing Tokens:**
- `--spacing-xs` (4px), `--spacing-sm` (8px), `--spacing-md` (16px), `--spacing-lg` (24px), `--spacing-xl` (32px), `--spacing-2xl` (48px), `--spacing-3xl` (64px)

**Color Tokens:**
- `--color-navy` (#001E5F) — Primary text and backgrounds
- `--color-yellow` (#FFD82B) — Accent borders and highlights
- `--color-meliora-blue` (#0045A0) — Hover states on links
- `--color-white` (#FFFFFF) — Text on navy backgrounds
- `--color-dark-gray` (#707070) — Secondary text (eyebrows, labels)
- `--color-border` (#E8E8E8) — Card borders
- `--color-butter` (#FFFBF0) — Warm background tint

**Breakpoints (reference, not in @media vars):**
- 640px — Mobile to tablet
- 768px — Nav collapses/expands
- 1024px — Tablet to desktop

## HTML Structure Pattern (from research.html and teaching.html)

```html
<!-- PAGE HEADER — navy background -->
<section class="section section--navy" style="min-height: 35vh;">
  <div class="container">
    <h1>Page Title</h1>
    <p>Lead text description</p>
  </div>
</section>

<!-- CONTENT SECTION -->
<section class="section">
  <div class="container">
    <!-- Content here -->
  </div>
</section>

<!-- CARD PATTERN -->
<div class="card card--highlighted">
  <h3>Card Title</h3>
  <p>Card content and description</p>
</div>
```

## Tab Interface ARIA Pattern (from W3C APG)

```html
<!-- Tab container -->
<div role="tablist" class="tabs">
  <button role="tab" aria-selected="true" aria-controls="panel-1">Tab 1</button>
  <button role="tab" aria-selected="false" aria-controls="panel-2">Tab 2</button>
  <!-- ... -->
</div>

<!-- Panel containers -->
<div role="tabpanel" id="panel-1">Content 1</div>
<div role="tabpanel" id="panel-2" hidden>Content 2</div>
```

(Note: tabindex values will be managed by JavaScript in Plan 02)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS tab interface styling to style.css</name>
  <files>css/style.css</files>
  <read_first>
    css/style.css (read full file to understand existing component architecture and spacing tokens)
    .planning/phases/04-students-hub/04-CONTEXT.md (tab styling locked decisions)
  </read_first>
  <action>
Append new CSS section to the end of css/style.css for tab interface styling. Add approximately 80-120 lines covering:

1. **`.tabs` container** (flex layout):
   - `display: flex`
   - `flex-wrap: wrap` (allow wrapping on very small screens if needed)
   - `gap: var(--spacing-md)` (16px gap between pill buttons)
   - `margin-bottom: var(--spacing-lg)` (space between tabs and panels)
   - Responsive: Full width on mobile, centered container on desktop

2. **`[role="tab"]` tab button styling** (pill buttons):
   - Inherit from `.btn` base styling or create new button pattern
   - Active tab (`aria-selected="true"`):
     - `background-color: var(--color-navy)` (#001E5F)
     - `color: var(--color-white)` (#FFFFFF)
     - `border-radius: 20px` (pill-shaped, fully rounded)
     - `padding: 0.75rem 1.75rem` (same as .btn for consistency)
     - `font-weight: 700` (Inter 600)
     - `border: 2px solid var(--color-navy)`
   - Inactive tab (`aria-selected="false"`):
     - `background-color: transparent`
     - `color: var(--color-navy)`
     - `border: 2px solid transparent` (maintain alignment)
     - `border-radius: 20px` (pill-shaped)
   - Hover state (for inactive tabs):
     - `background-color: transparent` (stay transparent)
     - `color: var(--color-meliora-blue)` (#0045A0) — Meliora Blue on hover
     - `border-color: var(--color-meliora-blue)` (optional subtle border)
     - `transition: color 0.2s ease, border-color 0.2s ease`
   - Focus state:
     - `outline: 2px solid var(--color-meliora-blue)` (or let browser default, per Claude's discretion)
     - `outline-offset: 2px`
   - Font/sizing:
     - `font-family: var(--font-sans)` (Inter)
     - `font-size: var(--font-size-label)` (0.875rem)
     - `font-weight: 700`
     - `min-height: 44px` (touch target RESP-06)
     - `cursor: pointer`
     - `transition: all 0.2s ease`

3. **`[role="tabpanel"]` panel styling**:
   - `display: block` or `display: none` for panels marked `hidden` (JavaScript will manage)
   - `padding: var(--spacing-3xl) 0` on mobile (64px vertical, inherits container padding)
   - On desktop (1024px+): Increase padding or keep consistent
   - `background-color: var(--color-white)` (white background)
   - `border-top: none` (panels are in white section, not bordered)
   - Panels will inherit color and typography from body/section defaults
   - Add minimal styling — let content (cards, prose) shape the panel

4. **Panel opacity/fade classes** (for animation in Plan 02):
   - `[role="tabpanel"][data-fade="out"]` — Not yet used in Plan 01, but define for Plan 02:
     - `opacity: 0`
     - `transition: opacity 0.2s ease-out`
   - `[role="tabpanel"][data-fade="in"]` — Not yet used in Plan 01, but define for Plan 02:
     - `opacity: 1`
     - `transition: opacity 0.2s ease-in`
   - Wrap fade animations in `@media (prefers-reduced-motion: no-preference) { ... }` so users with reduced motion preference get instant transitions (no fade)

5. **Responsive tab layout**:
   - Tabs stay horizontal on all screen sizes (320px+)
   - On narrow mobile (320-640px): Tabs may wrap to 2 rows if very tight, but three short labels should fit on one line
   - Use `flex: 0 1 auto` on tab buttons to prevent unwanted stretching
   - Container `.tabs` stays inside `.container` (inherits max-width 1100px)

CRITICAL NOTES:
- Do NOT use `.btn` class on tabs — tabs are semantic `<button role="tab">`, styled independently as pills
- All colors must use CSS custom properties (no hardcoded hex values except in comments)
- Spacing must use `--spacing-*` tokens
- Animation must be wrapped in `@media (prefers-reduced-motion: no-preference)`
- Tab buttons must be minimum 44x44px for touch accessibility (RESP-06)
- No transforms or scaling in transitions (only opacity + color for accessibility per decision)
  </action>
  <verify>
    <automated>grep -n "\[role=\"tab\"\]" css/style.css | head -1</automated>
    <automated>grep -n "\.tabs" css/style.css | head -1</automated>
    <automated>grep -n "prefers-reduced-motion" css/style.css | grep tabpanel</automated>
  </verify>
  <acceptance_criteria>
    - css/style.css contains new CSS section for tab styling (at least 80 lines of CSS)
    - `[role="tab"]` selector exists with navy background for active state, transparent for inactive
    - Hover state on inactive tabs uses Meliora Blue (#0045A0)
    - Focus state uses outline with color or default browser outline
    - `.tabs` container has `display: flex`, `gap: var(--spacing-md)`
    - `[role="tabpanel"]` has white background and padding
    - Fade animation classes exist but wrapped in @media (prefers-reduced-motion: no-preference)
    - All colors use CSS custom properties (--color-navy, --color-white, --color-meliora-blue, --color-yellow)
    - Tab buttons have minimum 44px height (touch target)
  </acceptance_criteria>
  <done>Tab styling CSS added to style.css; tab buttons render as navy pills (active) or transparent with navy text (inactive); hover states and focus indicators work; animation classes prepared for Plan 02</done>
</task>

<task type="auto">
  <name>Task 2: Create students.html with page header and three tab panels</name>
  <files>students.html</files>
  <read_first>
    index.html (reference page structure: nav, skip link, header/main/footer pattern)
    research.html (reference page header with navy background, H1, lead text)
    teaching.html (reference card layout and content organization)
    contact.html (reference multi-section layout)
    .planning/phases/04-students-hub/04-CONTEXT.md (content requirements and first-person voice rule)
    .planning/REQUIREMENTS.md (STU-01 through STU-12 for exact content)
  </read_first>
  <action>
Create new file `students.html` (copy header/nav/footer from index.html, modify content). Structure:

1. **DOCTYPE and <head> (copy from index.html)**:
   - UTF-8 charset
   - Viewport meta
   - Description meta
   - Title: "Students Hub — Dr. Irving Barron"
   - Link to css/style.css

2. **Skip link (copy pattern from index.html)**:
   - `<a class="skip-link" href="#main-content">Skip to main content</a>`

3. **Nav header (copy from index.html but update aria-current)**:
   - Brand/site name: "Dr. Irving Barron"
   - Nav links: Home, Research, Teaching, **Students (marked with aria-current="page")**, Contact
   - Hamburger button with aria-label, aria-expanded, aria-controls (keep intact)

4. **Main content (new)**:
   - `<main class="main-content" id="main-content">`

5. **PAGE HEADER section** (STU-01, STU-02):
   - `<section class="section section--navy" style="min-height: 35vh;">`
   - Inside `.container`:
     - `<h1>Students Hub</h1>` (white text on navy, inherited from .section--navy h1)
     - `<p class="eyebrow">Resources for current, prior, and prospective students</p>` (yellow eyebrow on navy, inherited from .section--navy .eyebrow)
   - No decorative elements (per Claude's discretion — keep header clean and focused)

6. **TAB INTERFACE section** (STU-03, STU-05):
   - `<section class="section">`
   - Inside `.container`:
     - `<div role="tablist" class="tabs">`
       - Three buttons with role="tab", aria-selected, aria-controls:
         1. `<button role="tab" aria-selected="true" aria-controls="panel-current">Current Students</button>`
         2. `<button role="tab" aria-selected="false" aria-controls="panel-prior">Prior Students</button>`
         3. `<button role="tab" aria-selected="false" aria-controls="panel-prospective">Prospective Students</button>`

7. **TAB PANELS** (STU-08, STU-09, STU-10, STU-11, STU-12):

   **Panel 1: Current Students** (id="panel-current", aria-labelledby="tab-current"):
   - Visible initially (no `hidden` attribute)
   - Content structure (semantic, first-person voice):
     - Office hours section (descriptive prose or definition list)
       - Example: "I hold office hours [days/times]. I'm happy to discuss coursework, research opportunities, or career advice."
       - (User to provide actual times; placeholder acceptable for now)
     - Course Materials: Blackboard link
       - `<p><a href="https://blackboard.com" target="_blank">Access course materials on Blackboard</a></p>`
     - Reaching Me: Email contact
       - `<p>Email me directly at <a href="mailto:ibarron@ur.rochester.edu">ibarron@ur.rochester.edu</a></p>`
     - Accommodations: Text about accessibility accommodations
       - Plain text or prose describing how to request accommodations
       - First-person: "If you need accommodations..." or "I work with..."

   **Panel 2: Prior Students** (id="panel-prior", aria-labelledby="tab-prior", hidden):
   - Marked `hidden` (shown/hidden by JavaScript in Plan 02)
   - Content structure:
     - "Stay in Touch" section (prose)
       - Example: "I love hearing from former students! Feel free to reach out..."
     - `.card .card--highlighted` (yellow top border per STU-11):
       - Card title: "Recommendation Letters"
       - Bulleted list of requirements:
         - "Completed coursework with me"
         - "Submit request at least 3 weeks in advance"
         - "Email me with context (program, deadline, etc.)"
         - "I can typically commit to 2-3 letters per year"

   **Panel 3: Prospective Students** (id="panel-prospective", aria-labelledby="tab-prospective", hidden):
   - Marked `hidden` (shown/hidden by JavaScript in Plan 02)
   - Content structure:
     - `.card .card--highlighted` (yellow top border per STU-12):
       - Card title: "Undergraduate Research Opportunities"
       - Bulleted list of application requirements:
         - "Enrolled in ECE program or by permission"
         - "Minimum GPA 3.0"
         - "Available for [semester/year]"
         - "Interested in signal processing, security, or privacy applications"
       - Call to action: "Email me at ibarron@ur.rochester.edu with your resume and research interests."
     - Graduate Admissions link:
       - Prose + link: "For graduate program information, visit [University graduate admissions page]" or "Interested in graduate studies? Contact me to discuss opportunities."

8. **Footer** (copy from index.html):
   - Navy background, white text
   - Copyright text with dynamic year
   - Links to ECE department and other university resources (copy pattern from index.html footer)

FIRST-PERSON VOICE REQUIREMENT (from CLAUDE.md):
- All content uses "I", "my", "me" (never "Dr. Barron", "the professor", third-person)
- Examples:
  - "I hold office hours..." (not "Office hours are...")
  - "I work with students..." (not "Accommodations are available...")
  - "Email me at..." (not "Contact at...")
  - "I can typically..." (not "Letters can typically...")
  - "I love hearing from..." (not "Former students are encouraged...")

CONTENT PLACEHOLDER NOTES:
- Office hours: Use placeholder "[Days/Times — to be specified by user]" or ask user to provide; keep sentence structure: "I hold office hours [placeholder]"
- Accommodations text: Generic accessibility statement acceptable for now
- All links (Blackboard, mailto, graduate admissions) should be functional or placeholders that don't break

SEMANTIC HTML RULES:
- Use `role="tabpanel"` on panel divs (not sections, for clarity)
- Use `aria-labelledby` pointing to button id if buttons have ids (or omit if using aria-controls only)
- No extra nesting; keep panel divs as direct children of the tab section
- Headings (h2, h3, h4) should follow hierarchy: H1 in page header, H2 for section titles, H3 for card titles or subsections within panels

NAVIGATION REQUIREMENT:
- Update nav link for "Students" to have `aria-current="page"` (remove it from "Home")
- Keep other nav links unchanged
  </action>
  <verify>
    <automated>grep -n "Students Hub" students.html</automated>
    <automated>grep -c "role=\"tabpanel\"" students.html</automated>
    <automated>grep -c "aria-selected" students.html</automated>
    <automated>grep -n "aria-current=\"page\"" students.html | grep -i student</automated>
  </verify>
  <acceptance_criteria>
    - students.html file exists in project root
    - Contains nav with "Students" link marked aria-current="page"
    - Page header section with class="section section--navy" containing H1 "Students Hub"
    - Lead text "Resources for current, prior, and prospective students" (yellow eyebrow on navy background)
    - `<div role="tablist" class="tabs">` with three tab buttons
    - Each tab button has role="tab", aria-selected, aria-controls attributes
    - Three role="tabpanel" divs with ids matching aria-controls (panel-current, panel-prior, panel-prospective)
    - First panel (current) has no hidden attribute; panels 2 and 3 have hidden attribute
    - "Current Students" panel contains office hours placeholder or text, Blackboard link, email link, accommodations info
    - "Prior Students" panel contains .card .card--highlighted with "Recommendation Letters" title and bulleted requirements
    - "Prospective Students" panel contains .card .card--highlighted with "Undergraduate Research Opportunities" title, bulleted requirements, and call to action
    - All content uses first-person voice (I, my, me)
    - Footer copied from index.html, shows dynamic year
  </acceptance_criteria>
  <done>students.html created with complete page structure, semantic tab markup, three content panels with first-person voice content, and all required nav/header/footer elements</done>
</task>

</tasks>

<verification>
After executing both tasks:

1. **CSS validation**: Open any page in browser, open DevTools → Elements, find a tab button element, check computed styles confirm navy background (active) or transparent (inactive), Meliora Blue on hover
2. **HTML validation**: Run `html5validator students.html` (if available) or check W3C validator — should have zero errors
3. **Semantic check**: Use browser DevTools → Accessibility inspector, verify tablist, tab, and tabpanel roles are properly nested
4. **Visual check**: Open students.html in browser at 320px, 768px, 1024px viewports — confirm:
   - Navy header with H1 and lead text visible and readable
   - Three tab buttons visible without horizontal scrolling, properly spaced
   - Tab buttons styled correctly (active: navy bg + white text, inactive: transparent)
   - Panel content is visible and readable
   - Links are clickable (mailto:, Blackboard, etc.)
5. **Keyboard check**: Press Tab to focus first tab button, verify focus indicator is visible, press Escape to close mobile nav (inherited from nav.js)
</verification>

<success_criteria>
- students.html is W3C valid HTML5 with no errors
- CSS tab styling rules added to style.css (80+ lines)
- Three tab buttons render with correct styling (navy pills for active, transparent for inactive)
- Hover state on inactive tabs uses Meliora Blue
- Focus indicator visible on tab buttons
- Three panel divs with correct ARIA roles and IDs
- All content is semantic, readable, and uses first-person voice
- Page header displays correctly on all breakpoints (320px, 768px, 1024px)
- Tab buttons fit on one line without horizontal scroll at typical mobile widths (320px+)
- All links (mailto, Blackboard, etc.) are present and functional or properly structured
- Footer displays correctly with dynamic year
</success_criteria>

<output>
After execution, create `.planning/phases/04-students-hub/04-01-SUMMARY.md` documenting:
- Files modified (css/style.css, students.html)
- CSS classes added (.tabs, [role="tab"] styling, fade animation classes)
- Semantic markup (role="tablist", role="tab", role="tabpanel")
- Content panels created (Current, Prior, Prospective)
- First-person voice verified in all content
- W3C validation confirmation
- Next step: Plan 02 will add JavaScript interactivity (click handlers, fade animations, tabindex management, arrow key navigation)
</output>
