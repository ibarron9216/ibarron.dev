---
phase: 02-home
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
  - HOME-08
  - HOME-09
  - HOME-10
  - HOME-11

must_haves:
  truths:
    - "Home page loads without console errors at index.html"
    - "W3C HTML validation passes with zero errors"
    - "All four ROADMAP success criteria are verifiable in the HTML"
    - "Responsive layout tested at 320px, 768px, and 1024px confirms correct breakpoint behavior"
  artifacts:
    - path: "index.html"
      provides: "Complete home page with hero, about me, and highlight cards sections"
      min_lines: 140
    - path: "css/style.css"
      provides: "CSS design system with all Phase 2 component modifiers and responsive classes"
      contains: ".btn--primary--inverse, .card--highlighted--navy, .hero__grid, .section--spacious"
  key_links:
    - from: "index.html hero section"
      to: "research.html and students.html"
      via: "anchor tag href attributes"
      pattern: "href=\"research\\.html\"|href=\"students\\.html\""
    - from: "index.html About Me section"
      to: "assets/cv.pdf"
      via: "Download CV button href"
      pattern: "href=\"assets/cv\\.pdf\""
---

<objective>
Execute and verify the Phase 2 validation checkpoint. Run W3C HTML validation to ensure index.html is well-formed, then perform manual visual verification across key breakpoints to confirm all four ROADMAP success criteria are met and all HOME-01 through HOME-11 requirements are satisfied.

Purpose: Close the gap from Plan 02-02 which was skipped during execution. Gate check before Phase 3 content pages.
Output: W3C validation report + visual verification confirmation
</objective>

<execution_context>
@/Users/ibarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ibarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-home/02-CONTEXT.md
@.planning/phases/02-home/02-VALIDATION.md
@.planning/phases/02-home/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate index.html with W3C HTML5 checker</name>
  <files>index.html</files>
  <read_first>
    - index.html (entire file to verify structure before validation)
    - css/style.css (verify classes are defined)
  </read_first>
  <action>
Run W3C Nu HTML Checker on index.html to ensure no validation errors exist. The file was built in Plan 02-01 and fixed with heading hierarchy correction (h4→h3 in highlight cards). Use curl to submit to validator.w3.org API.

If W3C API is not accessible, perform manual HTML validation checks:
1. Verify DOCTYPE present: `<!DOCTYPE html>`
2. Verify no duplicate IDs in the document
3. Verify all anchor tags have proper href attributes
4. Verify `role="img"` and `aria-label` are on profile placeholder div
5. Verify `aria-hidden="true"` is on initials span
6. Verify no unclosed tags (grep for balanced `<section>`, `<div>`, etc.)
7. Verify all `&` entities are properly escaped as `&amp;`

Acceptance: Zero errors (warnings/infos are acceptable).
  </action>
  <verify>
    <automated>cd /Volumes/SN8100_2TB_A/Git/Code/ibarron.dev && curl -s -H "Content-Type: text/html; charset=utf-8" --data-binary @index.html "https://validator.w3.org/nu/?out=json" 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); errors=[m for m in d.get('messages',[]) if m['type']=='error']; print(f'Validation Errors: {len(errors)}'); [print(f'  Line {e.get(\"lastLine\",\"?\")}: {e[\"message\"]}') for e in errors]; sys.exit(1 if errors else 0)" && echo "✓ PASS: HTML valid" || echo "✗ FAIL: See errors above"</automated>
  </verify>
  <acceptance_criteria>
    - W3C Nu HTML Checker returns zero errors (as JSON response)
    - Warnings and info-level messages are acceptable
    - grep "DOCTYPE html" index.html returns match (DOCTYPE present)
    - grep -E "role=\"img\"|aria-label=\"Profile photo" index.html shows profile placeholder has accessibility attributes
    - grep "aria-hidden" index.html shows initials span is hidden from screen readers
  </acceptance_criteria>
  <done>index.html passes W3C HTML5 validation with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — confirm ROADMAP success criteria and responsive layout</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 success criteria section)
    - .planning/phases/02-home/02-02-PLAN.md (verification instructions)
  </read_first>
  <action>
Open index.html in a browser and verify all four ROADMAP success criteria are met:

**ROADMAP Success Criteria (Phase 2):**
1. Hero section shows "Dr. Irving Barron" in serif font on navy background with affiliation eyebrow, lead text, and two action buttons side-by-side (stacked on mobile)
2. Profile photo placeholder appears as navy rectangle with yellow "IB" initials in hero (right column on desktop, above text on mobile)
3. About Me section shows biographical paragraph and "Download CV (PDF)" button that links to assets/cv.pdf
4. Three highlight cards (Research, Teaching, Students) appear below, each with colored top border and working "Learn more"/"Visit hub" link

**Responsive Verification at Key Breakpoints:**

At 320px width (mobile):
- Hero: Single column — text above photo
- Buttons: "View My Research" and "For Students" stacked vertically
- Cards: Stacked 1-column, each card visible below the other
- All text readable, no horizontal overflow

At 768px width (tablet):
- Hero: STILL single column (not two-column yet — two-column only at 1024px)
- Cards: STILL single column
- This verifies the .hero__grid locked decision (1024px breakpoint, not 640px)

At 1024px width (desktop):
- Hero: Two columns — text left, photo right
- Photo appears as navy square with yellow border and yellow "IB" initials
- Buttons appear side-by-side
- Cards: Three columns
- Research and Teaching cards: yellow 4px top border
- Students card: navy 4px top border (HOME-11)

**Link Verification:**
- Click "View My Research" → should navigate to research.html (or show 404 if not yet created)
- Click "For Students" → should navigate to students.html
- Click "Download CV (PDF)" → should open/download assets/cv.pdf
- Click "Learn more" in Research card → should navigate to research.html
- Click "Learn more" in Teaching card → should navigate to teaching.html
- Click "Visit hub" in Students card → should navigate to students.html

**Accessibility Quick Check:**
- Tab through page — all interactive elements (nav links, buttons, card links) should have visible focus indicator
- Use browser DevTools to inspect profile placeholder — verify role="img" and aria-label are present
- Verify nav hamburger button is functional on mobile (<768px)
  </action>
  <what-built>Complete home page (index.html) with:
- Navy hero section (60vh min-height) with Dr. Irving Barron H1 in serif, affiliation eyebrow, lead text, two CTA buttons
- Profile photo placeholder (CSS-only navy square with yellow "IB" initials, yellow border, subtle shadow)
- Butter background About Me section with bio paragraph and Download CV button
- Three highlight cards (Research/Teaching with yellow top border, Students with navy top border) with descriptions and links</what-built>
  <how-to-verify>
1. **Open index.html in browser:**
   - Use Chrome, Firefox, or Safari
   - Open from filesystem (file:// URL)
   - Confirm page loads, no console errors

2. **Desktop view (1024px+):**
   - Verify hero has two columns (text left, photo right)
   - Verify "View My Research" is yellow button with navy text
   - Verify "For Students" is outline button with white border
   - Verify profile photo is navy rectangle with yellow "IB" and yellow border
   - Verify About Me has butter background
   - Verify three cards in a row
   - Verify Research/Teaching cards have yellow top border
   - Verify Students card has navy top border (darker than yellow)
   - Scroll to confirm all sections visible and spaced generously

3. **Mobile view (320-375px):**
   - Use Chrome DevTools or resize browser to ~375px
   - Verify hero is single column (photo below text, not beside)
   - Verify buttons stack vertically
   - Verify cards stack vertically (1-column)
   - Pinch/zoom test: all text readable, no horizontal overflow
   - Verify nav hamburger button appears and works

4. **Tablet view (768px):**
   - Resize to exactly 768px wide
   - Verify hero is STILL single column (NOT two-column yet)
   - Verify cards STILL single column
   - This is the critical test of the .hero__grid breakpoint decision

5. **Click tests:**
   - Click each button and card link
   - Verify href attributes are correct in DevTools inspector
   - Confirm navigation works or shows expected target page

6. **Accessibility:**
   - Open DevTools → inspect profile placeholder
   - Verify role="img" attribute present
   - Verify aria-label="Profile photo placeholder for Dr. Irving Barron" present
   - Press Tab key repeatedly — verify focus indicators visible on all buttons and links
  </how-to-verify>
  <verify>User types "approved" confirming all ROADMAP success criteria met and responsive layout verified</verify>
  <acceptance_criteria>
    - User confirms hero renders correctly at desktop (1024px+) with two columns
    - User confirms hero is single column at mobile (375px) with stacked buttons
    - User confirms hero is single column at tablet (768px) — NOT two-column until 1024px
    - User confirms all four ROADMAP success criteria are visually present:
      1. Hero section shows Dr. Irving Barron, affiliation eyebrow, lead text, two buttons
      2. Profile photo placeholder is navy with yellow "IB" and yellow border
      3. About Me section has bio and Download CV button with butter background
      4. Three highlight cards with correct border colors and working links
    - User confirms no visual regressions from Phase 1 (nav/footer still work)
    - User confirms no console errors in DevTools
  </acceptance_criteria>
  <done>User has typed "approved" confirming Phase 2 home page meets all visual, functional, and responsive requirements. Ready for Phase 3.</done>
  <resume-signal>Type "approved" to confirm Phase 2 is complete, or describe any issues that need fixing (visual bugs, layout problems, missing content, broken links, etc.)</resume-signal>
</task>

</tasks>

<verification>
Phase 2 gap closure complete when:
1. W3C HTML validation passes with zero errors (Task 1)
2. User confirms all four ROADMAP success criteria met visually (Task 2)
3. Responsive layout verified at 320px, 768px, 1024px (Task 2)
4. All HOME-01 through HOME-11 requirements satisfied
5. User has typed "approved" to signal completion
</verification>

<success_criteria>
- W3C Nu HTML Checker: 0 validation errors
- User visual approval: "approved" typed in Task 2 checkpoint
- Responsive breakpoints verified: 320px (stacked), 768px (single-column before 1024px breakpoint), 1024px (two-column hero, three-column cards)
- All ROADMAP success criteria confirmed visually
- Ready to proceed to Phase 3 (Content Pages)
</success_criteria>

<output>
After completion, create `.planning/phases/02-home/02-03-SUMMARY.md` documenting:
- W3C validation results
- User approval confirmation
- Responsive breakpoint verification results
- Readiness for Phase 3
</output>
