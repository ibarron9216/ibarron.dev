---
phase: 02-home
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
  - HOME-08
  - HOME-09
  - HOME-10
  - HOME-11

must_haves:
  truths:
    - "Hero section renders correctly at 320px, 768px, and 1024px viewports"
    - "All four ROADMAP success criteria are visually confirmed"
    - "HTML passes W3C validation with no errors"
  artifacts: []
  key_links: []
---

<objective>
Validate the home page visually and structurally. Confirm all ROADMAP success criteria are met, HTML validates, and responsive layouts work at key breakpoints.

Purpose: Gate check before moving to Phase 3 (Content Pages). Catches visual regressions or layout issues that automated grep checks cannot detect.
Output: Human-verified approval of Phase 2 home page.
</objective>

<execution_context>
@/Users/ibarron/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ibarron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-home/02-CONTEXT.md
@.planning/phases/02-home/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run W3C HTML validation on index.html</name>
  <files></files>
  <read_first>
    - index.html (verify it is complete and well-formed before validation)
  </read_first>
  <action>
Validate index.html for HTML5 compliance. Since this is a plain HTML project with no npm/build tools, use one of these approaches:

Option A (preferred): Use curl to submit to the W3C Nu HTML Checker API:
```bash
curl -s -H "Content-Type: text/html; charset=utf-8" --data-binary @index.html https://validator.w3.org/nu/?out=json 2>/dev/null | python3 -m json.tool
```

Option B (fallback if no internet): Manually inspect the HTML for common issues:
- Check DOCTYPE is present
- Check all tags are properly closed
- Check no duplicate IDs
- Check all `&amp;` entities are correct
- Check `role="img"` and `aria-label` are on the profile placeholder
- Check `aria-hidden="true"` is on the initials span

If any validation errors are found, fix them in index.html before proceeding.
  </action>
  <verify>
    <automated>curl -s -H "Content-Type: text/html; charset=utf-8" --data-binary @index.html "https://validator.w3.org/nu/?out=json" 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); errors=[m for m in d.get('messages',[]) if m['type']=='error']; print(f'Errors: {len(errors)}'); [print(f'  Line {e.get(\"lastLine\",\"?\")}: {e[\"message\"]}') for e in errors]; sys.exit(1 if errors else 0)" && echo "PASS: HTML valid" || echo "FAIL: HTML has errors"</automated>
  </verify>
  <acceptance_criteria>
    - W3C Nu HTML Checker returns zero errors for index.html
    - Any warnings are reviewed but do not block (info-level messages are acceptable)
  </acceptance_criteria>
  <done>index.html passes W3C HTML5 validation with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of home page at key viewports</name>
  <files></files>
  <action>
Present the home page to the user for visual verification. The user must confirm that:
1. Hero section displays correctly on desktop (two-column) and mobile (stacked)
2. Profile photo placeholder renders as navy square with yellow "IB" and yellow border
3. About Me section has butter background with bio and CV download button
4. Three highlight cards show correct top border colors and working links
5. Responsive behavior is correct at 320px, 768px, and 1024px
  </action>
  <what-built>Complete home page with hero section (navy background, Dr. Irving Barron heading, affiliation eyebrow, lead text, two CTA buttons, profile photo placeholder), About Me section (butter background, bio, Download CV button), and three highlight cards (Research and Teaching with yellow top border, Students with navy top border).</what-built>
  <how-to-verify>
    1. Open index.html in a browser (Chrome or Firefox)

    2. Desktop view (1024px+ wide):
       - Hero: Navy background, two columns — text left (eyebrow, H1, lead, buttons side-by-side), photo right (navy square with yellow "IB" and yellow border)
       - Hero min-height is approximately 60vh
       - "View My Research" button is yellow with navy text
       - "For Students" button is white outline on navy
       - About Me: Butter background, "About Me" eyebrow, "Researcher, Educator, Engineer" H2, bio paragraph, navy "Download CV (PDF)" button
       - Cards: Three columns, Research and Teaching have yellow top border, Students has navy top border
       - Each card has title, description, and underlined link ("Learn more" or "Visit hub")

    3. Mobile view (resize to ~375px or use DevTools):
       - Hero: Single column — text above photo, buttons stacked vertically
       - Profile photo scales down proportionally
       - Cards stack to single column
       - All text readable, no horizontal overflow

    4. Tablet view (resize to ~768px):
       - Hero still single column (NOT two-column — two-column only at 1024px+)
       - Cards still single column (three-column only at 1024px+)

    5. Click tests:
       - "View My Research" button navigates to research.html (may not exist yet — confirm href is correct)
       - "For Students" button navigates to students.html
       - "Download CV (PDF)" button opens/downloads assets/cv.pdf
       - "Learn more" links in Research and Teaching cards point to research.html and teaching.html
       - "Visit hub" link in Students card points to students.html

    6. Accessibility quick check:
       - Tab through the page — focus indicators visible on all interactive elements
       - Profile photo placeholder has proper role="img" (inspect in DevTools)
  </how-to-verify>
  <verify>User confirms visual correctness by typing "approved"</verify>
  <acceptance_criteria>
    - User confirms hero section displays correctly at desktop (1024px+) and mobile (375px)
    - User confirms hero is NOT two-column at tablet (768px) — stays single column until 1024px
    - User confirms all four ROADMAP success criteria are met visually
    - User confirms no visual regressions from Phase 1 (nav and footer still work correctly)
  </acceptance_criteria>
  <done>User has typed "approved" confirming the home page meets all visual and functional requirements.</done>
  <resume-signal>Type "approved" to confirm Phase 2 is complete, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. W3C HTML validation passes with zero errors
2. Human confirms all four ROADMAP success criteria are met:
   a. Hero shows name, affiliation, lead text, two buttons on navy background
   b. Profile photo placeholder shows as navy rectangle with yellow "IB" initials
   c. About Me shows bio paragraph and "Download CV (PDF)" button
   d. Three highlight cards with colored top borders and working links
3. Responsive layouts verified at 320px, 768px, and 1024px
</verification>

<success_criteria>
- Human types "approved" confirming the home page looks correct
- No visual regressions from Phase 1 (nav, footer still work)
- Ready to proceed to Phase 3 (Content Pages)
</success_criteria>

<output>
After completion, create `.planning/phases/02-home/02-02-SUMMARY.md`
</output>
